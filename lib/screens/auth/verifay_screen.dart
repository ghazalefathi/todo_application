import 'package:flutter/material.dart';

class VerifayScreen extends StatelessWidget {
  const VerifayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}